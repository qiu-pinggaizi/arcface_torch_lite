{
  "optimizer": "AdamW",
  "learning_rate": 0.001,
  "weight_decay": 0.0005,
  "batch_size": 64,
  "epochs": 200,
  "scheduler": "cosine",
  "warmup_epochs": 5,
  "early_stopping_patience": 50,
  "loss": "ArcFace",
  "loss_params": {
    "scale": 30.0,
    "margin": 0.5
  }
}